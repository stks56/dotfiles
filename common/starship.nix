{
  programs.starship = {
    enable = true;
    enableZshIntegration = true;

    settings = {
      # format = "$time" + "$all";
      # time = {
      #   disabled = false;
      #   format = "[\\\[$time\\\]]($style)";
      #   time_format = "%T";
      # };
    };
  };
}
